"use client";

import type { TemplatePreviewProps } from "@/templates/preview-types";

export function RadiantBloomPreview({ template, preview, images }: TemplatePreviewProps) {
  return (
    <div className="min-h-screen bg-white flex items-center justify-center text-2xl">
      Radiant Bloom Preview (Coming Soon)
    </div>
  );
}
